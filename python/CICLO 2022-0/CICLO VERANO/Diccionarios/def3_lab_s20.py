"""
¿Cómo acceder a los datos?
Para acceder los eleemntos del diccionario a través de la clave se utiliza []:
d= {'name': 'Zara', 'edad':7,'clase':'First'}
print("d['name']:",d['name'])
print("d['edad']:",d['edad'])

- En caso se quiera obtener únicamente las claves de un diccionario se puede hacer por medio de la función keys(). Para acceder a los valres, se debe utilizar la función values().

d = {'nombre':'Zara','edad':7}
print("Claves :%s" % d.keys())
print("Valores : %s" % d.values ())

"""