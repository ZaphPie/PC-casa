
# ¿Cómo abrir un archivo?
# Para abrir un archivo se usa la función open():
# <objeto_archivo> = open(<archivo>,<modo>)

"""
La instrucción anterior abre el <archivo> y devuelve un <objeto_archivo>. Si no existe <archivo>, se genera una excepción OSError.

<archivo>: ruta absoluta o relativa del archivo a abrir.
<modo>: es un string opcional para especificar el modo en que se desea abrir el archivo. El valor predeterminado es “r” (lectura en modo texto). 

"""