"""
- Simple. Cuando en la definición de la función sólo se llama a si misma una vez.
- Múltiple. Cuando en la definición de la función se realiza mas de una llamada a si misma.
- Directa. Cuando la función se llama a si misma (simple o múltiple).
- Indirecta. Cuando la función llama a otra función que, a su vez, llama a la función original.


"""